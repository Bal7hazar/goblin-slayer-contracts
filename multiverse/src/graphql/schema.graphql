query getFinishedGames {
    slayerModels(first: 10000) {
        edges {
            node {
                name
                tag
                title
                xp
                gold
            }
        }
    }
}
