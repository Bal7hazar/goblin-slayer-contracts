{
    "name": "slayer-verse",
    "version": "0.0.1",
    "workspaces": [
        "multiverse",
        "server"
    ],
    "devDependencies": {
        "lerna": "^8.0.0",
        "prettier": "^3.0.3",
        "tsup": "^8.0.1"
    },
    "scripts": {
        "prettier-check": "bun x prettier --check .",
        "prettier": "bun x prettier --write ."
    },
    "dependencies": {}
}
